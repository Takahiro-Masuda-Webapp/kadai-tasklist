<h1>id:<%= @task.id %>のメッセージ編集ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, 'タスク' %>
    <%= f.text_field :contet %>
    
    
    <%= f.submit '更新' %>
<% end %>

<%= link_to '一覧ページに戻る', tasks_path %>

